TODO

* Garbage Collection
* Mutable Cons Cells
* (letrec
* (begin
* (lambda x
* (lambda (x . y)
* (cond
* Varargs for +, -, *, etc.
* (and & (or
* Standard library.
* Smarter parser (unmatched (
* Strings
* Non-integers
* call/cc
* Test system.
* Tail call optimisation.
* Macros
* IO
* Compiler should produce VM ready code, replace mis-named Assemble.hs with disk serialisation/deserialisation.
* Support comments in vmcode files, and add #!Run lines to output.

DONE

* (let
* (let*
* Boolean literals
